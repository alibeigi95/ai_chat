import 'package:flutter/material.dart';

import 'my_bot.dart';

class MainPage extends StatelessWidget {
  const MainPage({super.key});

  @override
  Widget build(BuildContext context) => const ChatBot();
}
